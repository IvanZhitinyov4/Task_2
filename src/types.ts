export type TUserRegistrationInfo = {
    name: string;
    password: string;
    phoneNumber: string;
}

export type TUserLoginInfo = {
    name: 'string';
    password: 'string';
}

export type TCompanyInfo = {
    companyName: string;
    country: string;
}